import { spaceFactory } from './spaceFactory';
export const margin = (theme) => spaceFactory('margin', theme);
export const marginTop = (theme) => spaceFactory('margin-top', theme);
export const marginLeft = (theme) => spaceFactory('margin-left', theme);
export const marginRight = (theme) => spaceFactory('margin-right', theme);
export const marginBottom = (theme) => spaceFactory('margin-bottom', theme);