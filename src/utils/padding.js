import { spaceFactory } from '../utils/spaceFactory';
export const padding = (theme) => spaceFactory('padding', theme);
export const paddingTop = (theme) => spaceFactory('padding-top', theme);
export const paddingRight = (theme) => spaceFactory('padding-right', theme);
export const paddingBottom = (theme) => spaceFactory('padding-bottom', theme);
export const paddingLeft = (theme) => spaceFactory('padding-left', theme);
